[build-system]
requires = [
    'setuptools',
    'wheel',
    'scikit-build',
    'cmake',
]
build-backend = 'setuptools.build_meta'

[tool.ruff]
extend-exclude = [
    'extern',
]

[tool.ruff.format]

[tool.cibuildwheel.linux]
before-all = "yum install -y ninja-build"
